version: "3.3"
services:
    proxyserver:
        build:
            context: ./proxy
        ports:
            - "8080:80"
    theia:
        restart: always
        build:
            context: .
        ports:
            - "30000:3000"